// Define a variable
const language = "JavaScript";

// Define a function using ES6 arrow syntax
const createGreeting = (name) => {
  return `Hello, ${name}!`;
};

// Output the result to the console
console.log(createGreeting(language));
